# 섹터 뉴스 부스트 (스펙 B) — 포인터 (2026-09-06)

> **본문은 kis-trading-template 리포에 있다.** 이 파일은 사본이 아니라 위치 안내다. 사본을 두면 어긋난다.
>
> `D:\GIT\kis-trading-template\RoboTrader_template\docs\superpowers\specs\2026-09-06-sector-news-boost-design.md`

## 한 줄 요약

NewsQuant 가 **뉴스 → KSIC 3자리 섹터 점수**(`score_signed ∈ [−1, +1]`)를 10분마다 계산해 공유 DB `kis_template` 의 `sector_news_score` 표에 쓰고, 봇(kis-trading-template)이 **09:00 후보 로드 때** 그 점수로 매수후보 순위를 최대 3칸 움직인다. 처음엔 **shadow**(기록만)로 돌린다.

## NewsQuant 쪽에서 바뀌는 것 (스펙 §2.1)

| 파일 | 역할 |
|---|---|
| `news_scraper/data/sector_keywords.yaml` | KSIC3 별 한/영 키워드 사전(초기 49섹터) |
| `news_scraper/sector_news_aggregator.py` | 순수 로직(사전 매칭 · 종목매핑 · 집계). DB 를 모른다 |
| `news_scraper/database.py` | DDL 2표(`sector_news_score` · `news_sector_hit`) + upsert/read |
| `news_scraper/scheduler.py` | 10분 주기 잡 `sector_news_aggregation` (⚠️ 미커밋 diff 254줄 위) |
| `news_scraper/api/server.py` | `GET /api/sector/news-score` (점검용, 봇은 안 씀) |
| `requirements.txt` · `pyproject.toml` · `tests/` | `pyyaml`·`pytest` 추가, 테스트 디렉터리 신설 |

## 사장님 결정 (2026-09-06)

KSIC 3자리(스펙 A 위) · 키워드 사전 + 종목매핑 둘 다 · 09:00 로드 시 재정렬 · 양방향 · shadow 먼저 · NewsQuant 계산 → 공유 DB 표(1안).

## 의존

- 스펙 A(`2026-09-06-sector-data-design.md`)의 `fn_sector_map_as_of` — 종목매핑 경로(B)에만 필요. 키워드 경로(A)는 A 없이 동작.
- 현재(9/6) 스펙 A 는 `feat/sector-data` 워크트리에서 구현 중, DDL 은 DB 에 있으나 0행.

## 운영 메모 (범위 밖, 기록만)

- 마지막 수집 2026-09-04 20:36(금). 야간·주말에 프로세스가 죽어 있다 → 「밤사이 뉴스」는 프로세스 생존에 달려 있다.
