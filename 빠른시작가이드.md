# NewsQuant 빠른 시작 가이드

## 프로그램 실행 방법

### Windows 사용자 (추천) 🪟

#### 1. 패키지 설치
`install.bat` 파일을 더블클릭하거나 실행

#### 2. 프로그램 실행
- **전체 실행** (API + 스케줄러): `start.bat` 더블클릭
- **메뉴에서 선택**: `menu.bat` 더블클릭
- **API만 실행**: `start_api_only.bat` 더블클릭
- **스케줄러만 실행**: `start_scheduler_only.bat` 더블클릭

### 명령줄 사용자

#### 1. 설치
```bash
pip install -r requirements.txt
```

#### 2. 실행
```bash
python main.py
```

실행 시:
- ✅ 뉴스 수집 스케줄러 시작 (백그라운드)
- ✅ API 서버 시작: http://127.0.0.1:8000

---

## 다른 프로그램에서 사용하는 방법

### 기본 사용 (Python)

```python
import requests

API_BASE = "http://127.0.0.1:8000"

# 최신 뉴스 조회
response = requests.get(f"{API_BASE}/api/news/latest?limit=50")
data = response.json()
news_list = data["data"]

# 종목별 뉴스 조회
response = requests.get(f"{API_BASE}/api/news/stock/005930")
samsung_news = response.json()["data"]
```

### 실전 사용: 여러 종목 배치 조회 ⭐

```python
import requests

# 1차 필터링: 주식 프로그램에서 선택한 종목들
filtered_stocks = ["005930", "000660", "035420"]

# 2차 필터링: 배치 API로 여러 종목 한 번에 조회
response = requests.post(
    "http://127.0.0.1:8000/api/news/stocks/batch",
    json={
        "stock_codes": filtered_stocks,
        "limit_per_stock": 10,
        "min_score": 0.6
    }
)

data = response.json()

# 각 종목별 결과
for stock_code, result in data["results"].items():
    news_list = result["news"]
    count = result["count"]
    
    if count > 0:
        latest = news_list[0]
        sentiment = latest.get("sentiment_score", 0)
        print(f"{stock_code}: 감성 점수 {sentiment:.2f}")
```

---

## 주요 API 엔드포인트

| 엔드포인트 | 설명 | 예시 |
|-----------|------|------|
| `GET /api/news/latest` | 최신 뉴스 | `?limit=50&min_score=0.7` |
| `POST /api/news/stocks/batch` | 여러 종목 배치 조회 | `{"stock_codes": ["005930", "000660"]}` |
| `GET /api/news/stock/{code}` | 단일 종목 | `/api/news/stock/005930` |
| `GET /api/news/search` | 검색/필터링 | `?keyword=삼성&min_sentiment=0.5` |
| `GET /api/health` | 상태 확인 | 없음 |

---

## 응답 형식

```json
{
  "success": true,
  "count": 50,
  "data": [
    {
      "title": "뉴스 제목",
      "sentiment_score": 0.75,
      "overall_score": 0.82,
      "related_stocks": "005930,000660",
      ...
    }
  ]
}
```

---

## 브라우저에서 테스트

- API 문서: http://127.0.0.1:8000/docs
- 직접 테스트: http://127.0.0.1:8000/api/news/latest?limit=10

---

## 예제 코드

- `examples/api_client_example.py` - 기본 예제
- `examples/stock_trading_example.py` - 실전 거래 시스템 예제

---

## 핵심만 요약

### Windows 사용자
1. **설치**: `install.bat` 실행
2. **실행**: `start.bat` 또는 `menu.bat` 실행
3. **API 주소**: http://127.0.0.1:8000

### 명령줄 사용자
1. **설치**: `pip install -r requirements.txt`
2. **실행**: `python main.py`
3. **API 주소**: http://127.0.0.1:8000

### 공통
- **배치 조회**: `POST /api/news/stocks/batch` (여러 종목 한 번에)
- **API 문서**: http://127.0.0.1:8000/docs

**끝!** 🚀
